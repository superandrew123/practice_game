document.getElementById('score-board').style.display = "block";

display_game_status(<%= @score.to_json.html_safe %>);

<% if @conditions[:won] || @conditions[:board_locked] %>
  create_GameStats({});
<% end %>